
# system

Configurações a nível de sistema (fora do `~/.config`).

- `logind.conf.d/10-lid.conf` — faz o notebook **ignorar o fechamento da tampa quando em energia externa** (não suspende ao fechar o lid plugado)

