# kitty

Configuração do Kitty.

- `kitty.conf` — importa cores do tema atual do Omarchy (`~/.config/omarchy/current/theme/kitty.conf`), fonte JetBrainsMono Nerd Font tam. 9, padding 14, `background_opacity 0.5` (transparência forte).
