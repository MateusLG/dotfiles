# ghostty

Configuração do Ghostty.

- `config` — importa cores do tema atual do Omarchy (`~/.config/omarchy/current/theme/ghostty.conf`), fonte JetBrainsMono Nerd Font tam. 9, padding 14, `background-opacity = 0.5` (transparência forte).
