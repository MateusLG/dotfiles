
# scripts

Scripts utilitários.

- `work.sh` — conecta na VPN da $VPN_PROFILE (FortiClient) e abre a sessão RDP no Remmina automaticamente
- `lgfetch.sh` — wrapper sobre o `fastfetch` que troca o logo e a cor de destaque conforme o tema atual do Omarchy (lido de `~/.config/omarchy/current/theme.name`): oak verde para `lg-umbra`, tubarão "Sammy" azul para `lg-abissal`, fastfetch padrão nos demais. Instalado em `~/.local/bin/lgfetch`.

