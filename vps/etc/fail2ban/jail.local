[DEFAULT]
bantime  = 1h
findtime = 10m
maxretry = 5
backend  = systemd

[sshd]
enabled = true
port    = 22
# Ubuntu: o unit é ssh.service, não sshd.service. Sem isto o jail nunca casa ataques reais.
journalmatch = _SYSTEMD_UNIT=ssh.service + _COMM=sshd
