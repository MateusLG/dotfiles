# Derruba conexões mortas (mobile que trocou de rede / dormiu) do lado do servidor.
# tmux + mosh mantêm a sessão viva mesmo assim. Ver vps/mobile-claude.md.
ClientAliveInterval 60
ClientAliveCountMax 3
