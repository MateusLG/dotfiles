general {
    lock_cmd = omarchy-lock-screen                         # lock screen and 1password
    before_sleep_cmd = loginctl lock-session               # lock before suspend.
    after_sleep_cmd = sleep 1 && hyprctl dispatch dpms on  # delay for PAM readiness, then turn on display.
    inhibit_sleep = 3                                      # wait until screen is locked
}

listener {
    timeout = 1800                                           # 30min
    on-timeout = pidof hyprlock || omarchy-launch-screensaver # start screensaver (if we haven't locked already)
}

listener {
    timeout = 1830                                                                                  # 30min30s (30s após o protetor)
    on-timeout = loginctl lock-session && brightnessctl -sd '*::kbd_backlight' set 0                # lock + desliga backlight do teclado
    on-resume = brightnessctl -rd '*::kbd_backlight'                                                # restaura backlight ao retomar
}

listener {
    timeout = 1860                                           # 31min (30s após o lock)
    on-timeout = hyprctl dispatch dpms off                   # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r # screen on when activity is detected
}
